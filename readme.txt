# Installing tool chain
sudo apt-get install g++-arm-linux-gnueabihf

# Getting the source from wild dog
git clone git@wilddog.local:/opt/git/project.git

# Pushing updates
git rm ...
git add ...
git commit -m "message"
git push

# Make 
make -f makefile

